{
  "name": "microservices-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop all",
    "restart": "pm2 restart all",
    "logs": "pm2 logs",
    "install:all": "npm --prefix email-service install && npm --prefix user-service install && npm --prefix order-service install"
  },
  "dependencies": {},
  "devDependencies": {}
}
